name: Test on Google Cloud VM

on: [push]

jobs:
  test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Run PHP Artisan Test
        run: |
          cd src
          php artisan test