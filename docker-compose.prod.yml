services:
  web:
    restart: always

  nginx:
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./docker-config/nginx/default.conf.prod:/etc/nginx/conf.d/default.conf
    restart: always

  mariadb:
    restart: always